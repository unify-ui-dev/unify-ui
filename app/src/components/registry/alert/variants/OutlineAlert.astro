---
import type { VariantIntent } from "@/types/index";

type Props = {
    title: string;
    icon?: string;
    description: string;
    intent: VariantIntent;
};
const { title, icon = "i-ph-info", description, intent } = Astro.props;
---

<div
    class:list={[
        "border rd-md p-4 flex items-start gap-x-4",
        {
            "ui-outline": intent === "gray",
            "ui-outline-primary": intent === "primary",
            "ui-outline-secondary": intent === "secondary",
            "ui-outline-accent": intent === "accent",
            "ui-outline-success": intent === "success",
            "ui-outline-danger": intent === "danger",
            "ui-outline-warning": intent === "warning",
            "ui-outline-info": intent === "info",
            "ui-outline-neutral": intent === "neutral",
        },
    ]}
>
    <div class="w-max">
        <span class={`flex text-lg ${icon}`}></span>
    </div>
    <div class="flex-1">
        <h3
            class:list={[
                "font-medium text-lg",
                {
                    "text-title": intent === "gray",
                    "text-primary-600": intent === "primary",
                    "text-secondary-600": intent === "secondary",
                    "text-accent-600": intent === "accent",
                    "text-success-600": intent === "success",
                    "text-info-600": intent === "info",
                    "text-warning-600": intent === "warning",
                    "text-danger-600": intent === "danger",
                    "text-gray900 dark-text-white": intent === "neutral",
                },
            ]}
        >
            {title}
        </h3>
        <p>
            {description}
        </p>
    </div>
</div>
