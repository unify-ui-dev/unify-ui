---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"div"> & {}

const { class: className = "", ...rest } = Astro.props;
---

<div class={`mx-auto wfull max-w7xl px4 sm-px8 ${className}`} {...rest}>
  <slot />
</div>
