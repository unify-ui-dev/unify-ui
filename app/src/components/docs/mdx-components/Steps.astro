<ol class="steps space-y-2 mt6 first-mt0">
    <slot />
  </ol>
  
  <style is:global>
    .steps {
      counter-reset: step;
      position: relative;
    }
    .step {
      position: relative;
      padding-left: 2.5rem;
      padding-bottom: 2rem;
      counter-increment: step;
    }
    .step:last-child {
      padding-bottom: 0rem;
    }
    .step::before {
      position: absolute;
      display: flex;
      content: counter(step);
      left: 0;
      align-items: center;
      justify-content: center;
      width: calc(1.5rem + 1px);
      height: calc(1.5rem + 1px);
      font-size: 0.675rem;
      font-weight: 400;
      border-radius: 0.375rem;
    }
  
    .step::after {
      position: absolute;
      top: calc(1.875rem + 1px);
      left: 0.6875rem;
      bottom: 0;
      content: "";
      width: 1px;
    }
    .step:last-child::after {
      display: none;
    }
  </style>
  