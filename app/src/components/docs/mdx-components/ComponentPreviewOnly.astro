---
import dynamic from "astro:import";
import BlockCode from "./BlockCode.astro";

type Props = {
    name: string;
    props?: any;
};

const { name, props = {} } = Astro.props;

const toImport = `components/registry/${name}.astro`;
const Component = await dynamic(toImport);
---

<div class="space-y-5 my5 first-mt0">
    <div
        data-pagefind-ignore
        class="b bdr-high rd-lg p1.5 md-p10 bg-body d-flex-justify-center min-h-max"
    >
        <Component {...props} />
    </div>
    <BlockCode>
        <slot />
    </BlockCode>
</div>
